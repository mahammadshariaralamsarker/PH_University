// import { TErrorSources } from "../interface/error";

/* export const handleDuplicateID = (err) =>{
  
const errorSources:TErrorSources = [{
  path:err.keyValue.name,
  message:err.message
}
]
const statusCode = 400;
return{
  statusCode,
  message:'Invalid ID',
  errorSources,
}

} */
